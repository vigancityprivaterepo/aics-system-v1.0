services:
  postgres:
    container_name: aics-system-postgres
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aics_db
    volumes:
      - aics_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d aics_db"]
      interval: 10s
      timeout: 5s
      retries: 10

  backend:
    container_name: aics-system-backend
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - ./backend/.env
    volumes:
      - aics_uploads:/app/backend/uploads
    ports:
      - "5101:5001"

  frontend:
    container_name: aics-system-frontend
    build:
      context: ./frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "8081:80"

  portal:
    container_name: aics-system-portal
    build:
      context: ./portal
      args:
        VITE_GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "80:80"

volumes:
  aics_pg_data:
  aics_uploads:
